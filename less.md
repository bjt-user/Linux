Just like in vim you can go one page forward with:\
<kbd>ctrl</kbd> + <kbd>f</kbd>\
and backwards with:
<kbd>ctrl</kbd> + <kbd>b</kbd>

show line numbers:
```
less -N myfile.txt
```

#### searching

forward search
```
/pattern
```

backwards search
```
?pattern
```
