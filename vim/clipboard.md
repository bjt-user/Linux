## enable clipboard

Make sure `clipboard` feature is enabled:
```
vim --version
```

## copy from clipboard

Hit `"`, `+`, `p/P`.

## copy to clipboard

Hit `"`, `+`, `y/Y`.

## keybindings

These bindings might be useful:
```
vnoremap <C-c> "+y
noremap <C-v> "+P
```
