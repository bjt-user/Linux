The execute command is often necessary to concatenate strings and execute them \
as a command.\
Because variables don't just expand in a lot of commands.

example:
```
execute 'highlight link my_group '. my_var
```
