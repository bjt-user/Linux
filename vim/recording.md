Often you accidentally switch into `recording` mode.

It is started by hitting `q` and then another key.

You can quit it with `q`.

#### weird behavior

When at the end of a more prompt you hit `q` instead of `enter` \
then this will close the more prompt, but also trigger the `recording`.
