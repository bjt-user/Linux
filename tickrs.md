## general info

#### installation

```
sudo pacman -S tickrs
```

## configuration

The config file is at `${HOME}/config/tickrs/config.yml`.

#### start in summary mode

To show more stocks on one screen:
```
# Start in summary mode
summary: true
```
